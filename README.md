# HyperV-ADDS-DNS-DHCP-Redundancy-Lab
In this lab I configure three Windows Server 2022 machines: one as a primary AD DS, DNS, and DHCP server, one as a secondary domain controller with DNS, and one as a backup DHCP server to provide redundancy and high availability.
